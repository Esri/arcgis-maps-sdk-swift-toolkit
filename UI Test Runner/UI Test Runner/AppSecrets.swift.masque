// Copyright 2023 Esri
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import ArcGIS

extension String {
    static let formViewTestDataCase_1_x: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_CASE_1_x }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataCase_1_4: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_CASE_1_4 }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataCase_2_x: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_CASE_2_x }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataCase_3_x: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_CASE_3_x }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataCase_4_x: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_CASE_4_x }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
   static let formViewTestDataCase_5_x: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_CASE_5_x }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataDomain: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_DOMAIN }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataPassword: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_PASSWORD }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
    
    static let formViewTestDataUsername: String? = {
        let secret = "{{ FORM_VIEW_TEST_DATA_USERNAME }}"
        guard !secret.isEmpty else {
            return nil
        }
        return secret
    }()
}
