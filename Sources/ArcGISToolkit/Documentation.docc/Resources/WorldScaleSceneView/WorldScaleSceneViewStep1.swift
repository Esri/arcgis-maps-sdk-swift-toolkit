import ArcGIS
import ArcGISToolkit
import SwiftUI

struct WorldScaleExampleView: View {
    var body: some View {
        WorldScaleSceneView(provider: Provider) { _ in

        } sceneView: { _ in

        }
    }
}
